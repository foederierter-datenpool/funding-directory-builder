@prefix :    <https://civic-data.de/pipeline#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .

# ---- Same-as assertions -------------------------------------------------

# Manual owl:sameAs pairs go here once cross-source matching of the same funding
# programme is enabled (none yet — the match rule currently keeps every record
# its own cluster).

# ---- Stop-token lists ---------------------------------------------------

# Stop-token lists (:StopTokenList with :token …) for name-similarity matching
# go here when match criteria are added.
