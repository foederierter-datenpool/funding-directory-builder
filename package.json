{
  "name": "funding-directory-builder",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "pipeline": "directory-builder",
    "ingest": "directory-builder ingest",
    "federate": "directory-builder federate",
    "validate": "directory-builder validate",
    "webapp": "directory-builder webapp",
    "webapp:build": "directory-builder webapp build",
    "test": "node --test test/*.test.js",
    "test:update": "UPDATE_FIXTURES=1 node --test test/extract.test.js",
    "test:coverage": "node test/coverage.js"
  },
  "dependencies": {
    "@directory-builder/core": "^0.3.8",
    "fflate": "^0.8.3"
  }
}
