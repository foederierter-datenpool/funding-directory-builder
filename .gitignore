.DS_Store
.idea/
_local/

node_modules/

data/
tools/

# built webapp (npm run webapp:build) — the engine owns webapp/, these are its artifacts
/webapp/dist/
/webapp/package-lock.json

# Large static data files
static-sources/
